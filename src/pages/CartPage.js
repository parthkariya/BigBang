import React from "react";

const CartPage = () => {
  return (
    <section
      style={{
        width: "100%",
        height: "100vh",
        background: "#ffeeef",
        marginTop: "200px",
      }}
    ></section>
  );
};

export default CartPage;
